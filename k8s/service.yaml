apiVersion: v1
kind: Service
metadata:
  name: devops-local-pipeline-lab
  labels:
    app: devops-local-pipeline-lab
spec:
  type: ClusterIP
  selector:
    app: devops-local-pipeline-lab
  ports:
    - name: http
      port: 80
      targetPort: http
